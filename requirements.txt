# Flask and Core Dependencies
Flask==3.1.0
Werkzeug==3.1.1      # Dependency for Flask
Jinja2==3.1.5        # Dependency for Flask templating
itsdangerous==2.2.0  # Dependency for Flask sessions/signing
click==8.1.8         # Dependency for Flask CLI
MarkupSafe==3.0.2    # Dependency for Jinja2

# Flask Extensions (used by the app)
Flask-Bcrypt==1.0.1
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1

# Database (SQLAlchemy and PostgreSQL driver)
SQLAlchemy==2.0.40
greenlet==3.1.1      # Often needed by SQLAlchemy/drivers
psycopg2-binary      # PostgreSQL driver

# PDF and OCR
PyPDF2
easyocr
pillow==11.1.0       # Image handling, required by easyocr
numpy==2.2.3         # Numerical processing, required by easyocr/torch

# Google GenAI Client and Dependencies
google-genai==1.2.0
requests==2.32.3     # Often used by API clients like google-genai
pydantic==2.11.0a2   # Required by google-genai
pydantic_core==2.29.0 # Required by pydantic
annotated-types==0.7.0 # Required by pydantic

# Deployment
gunicorn==23.0.0

# Utilities
python-dotenv        # For loading .env files
typing_extensions==4.12.2 # Often required by pydantic, fastapi, etc.
pytz==2025.2          # Timezone support, often used with datetime/SQLAlchemy

# --- Potentially Unnecessary ---
# websockets==14.2 # Remove unless your app specifically uses WebSockets
